- [AI Agent Faz 2 RAG pipeline](ai-agent-rag-pipeline.md) — extract→chunk→embed→retrieve into bot prompt; NO pgvector (Hostinger prod lacks it) — jsonb embeddings + Node-side cosine; pdf-parse v2 uses PDFParse class not default export.
- [notifications table cascade-delete timeout](notifications-table-cascade-delete-timeout.md) — deleting users times out via FK cascade (only partial index on user_id); pre-existing, unrelated to whatever feature touched users.
- [Aggregator dynamic membership + auto-drain gating](aggregator-dynamic-membership-gating.md) — DB member list unions with (never replaces) static allowlist; blanket experimental-family auto-drain exclusion is redundant once enqueue already scopes by DB membership.
- [Staff "handled countries" Faz 1 foundation](staff-countries-faz1.md) — staff_countries mirrors staff_languages but via boot DDL (sibling tables aren't, may be missing in prod); helper in api-server/src/lib/staffCountries.ts for Faz 2 reuse.
- [Stuck-conversation auto-assign (Faz 2)](staff-stuck-conversation-assign-faz2.md) — immediate handoff hook (botAutoReply) + periodic sweep share one core; 3 settings toggles gate working-hours/country/off-hours; testing hits the REAL staff pool.
- [Portal program-fallback rules](portal-program-fallbacks.md) — soft-delete needs PARTIAL unique index (WHERE deleted_at IS NULL) or recreate 500s; supersede links live on applications (join in submissions list); program picker must paginate (100 cap).
- [Portal claim stage-gating](portal-claim-stage-gating.md) — claimNext(worker,uniKeys?,triggerStages?); undefined=skip filter (manual process-queued), array (even []) applies EXISTS on applications.stage, []=match nothing; run-now/drain-once/worker pass settings.triggerStages.
- [VPS storage driver abstraction](vps-storage-driver.md) — STORAGE_DRIVER=local/replit; LocalStorageFile duck-types GCS; prod active fileKeys = 56 unique (65 rows, 9 shared); export script in scripts/export-object-storage.ts.
- [Vite ships render-time ReferenceErrors](vite-runtime-referenceerror.md) — esbuild strips types without scope-checking; out-of-scope refs only fail at runtime → ErrorBoundary reload loop. Run tsc.
- [Photo doc filter & PDF avatar](photo-doc-filter-pdf-avatar.md) — Document API doesn't expose fileData; filter must use fileKey||fileUrl; PDF photos need pdfjs-dist canvas render (lazy) not <img>; photo endpoint needs fileUrl redirect with SSRF guard.
- [Dashboard FAZ 1 entity_view_events](dashboard-faz1-entity-view.md) — entity_view_events boot DDL in api-server Step 2b4; activityV1.ts for POST /v1/activity/view (5-min dedup) + GET /v1/activity/summary; Kommo in stats.ts; lib/db needs tsc -b after schema add.
- [Schema/OpenAPI changes need lib dist rebuild + safe DB migration](monorepo-schema-codegen-build.md) — rebuild lib dist (tsc -b) after schema/codegen; use ALTER TABLE not drizzle push (push drops unrelated tables).
- [List endpoints need ORDER BY with LIMIT](list-pagination-stability.md) — row vanishing from a paginated list after edit = LIMIT without stable ORDER BY; edit-dialog preload fields must be in the list select.
- [Test fragment uniqueness vs leaked rows](test-fragment-uniqueness.md) — test search fragments must include RUN_ID (per-run unique); fixed prefixes accumulate leaked rows across failed runs → LIMIT pushes seeded row off page.
- [node:test v24 nested before() concurrency](node-test-nested-before-concurrency.md) — describe-level before() hooks run concurrently in node:test v24; shared state created inside them races. Fix: hoist shared resource creation to root before().
- [Direction-aware null-last sort](direction-aware-null-last-sort.md) — don't negate a null-aware comparator for desc (floats nulls to top); handle nulls unconditionally, flip only the value compare; normalize missing to null not "".
- [edcons record assignment & reassign gating](edcons-record-assignment.md) — assign_button=assign/self-claim unassigned; change_assigned=reassign assigned; gate UI with `assignedToId ? canReassign : canAssign`.
- [Custom dropdown closes on scrollbar drag](dropdown-scrollbar-dismiss.md) — outside-close must listen to "click" not "mousedown"; preserve selected value as fallback option when filtering option lists.
- [Contract PDF rendering](contract-pdf-rendering.md) — templates are designed for browser-to-PDF; signed/preview PDF must render real HTML via headless Chromium (playwright-core+Nix), inject signer sig into {{signature}}, SSRF-filter subresources. Never pdf-lib text.
- [Contract PDF render → opaque 403](contract-pdf-render-403.md) — never run headless-Chromium PDF render synchronously NOR as post-response background work on autoscale (OOM-crashes → proxy 403 HTML). Render lazily on first download, serialize (withRenderLock); dev hides it.
- [universities endpoint limit cap](edcons-universities-limit-cap.md) — GET /api/universities silently caps limit at 100; selectors needing the full list must paginate, not request one big limit.
- [staff picker role filtering](edcons-staff-picker-roles.md) — assign/staff dropdowns must fetch /api/users?roles=<csv> server-side; unfiltered fetch + client whitelist silently truncates (students dominate the first page).
- [Active-program-only country/uni lists](edcons-active-program-filters.md) — for Course Finder parity (exclude empty destinations) source dropdowns from /api/course-finder/filters, NOT /api/universities*.
- [Prod→Dev data copy](prod-to-dev-data-copy.md) — prod read-only via executeSql, no prod creds in dev; base64-JSON + json_populate_recordset + replica role; always verify per-table count(*), bulk copy silently skips tables.
- [Notification template dispatch](notification-template-dispatch.md) — multilingual templates: field-level fallback (lang→top-level→en→tr), escape vars in HTML email bodies, per-channel try/catch, await the error-safe dispatch.
- [i18n key parity](i18n-key-parity.md) — locale JSONs must contain EVERY en string key (getTranslation falls back to en silently); don't letter-filter the parity/fill step, only the translate step.
- [Dropdown overflow clipping](dropdown-overflow-clipping.md) — absolute+z-index doesn't escape ancestor overflow; portal the menu (fixed pos from rect, flip/clamp, click outside-close).
- [Popover/cmdk inside a Dialog breaks](dropdown-popover-in-dialog.md) — Radix Popover+cmdk in a Dialog silently kills typing/click (FocusScope+pointer-events:none); portal the menu INTO dialog-content, fixed pos, reset on close.
- [Admin-contract deadline suspension](admin-contract-deadline-suspension.md) — suspend agent (users.isActive=false) on missed admin_driven contract deadline; filter mode=admin_driven AND gate suspend on conditional-expire rowcount or you suspend signed-in-time accounts.
- [Fast Refresh mixed exports crash](fast-refresh-mixed-exports.md) — a .tsx exporting a component + a hook/util/type breaks Fast Refresh → full reload can crash mounted shells (empty {} ErrorBoundary). Dev-only; not the prod stale-chunk case.
- [Lifted child submit/readiness](lifted-child-submit-ref.md) — moving a child's action button to a parent footer via ref+state stale-enables across mounts; clean up on unmount + reset on view change.
- [Public sign email verification](public-sign-email-verification.md) — signer-email 6-digit gate: bind codes to the link via hashToken in the `token` column; render verify widget in BOTH intake (self_fill) and sign (admin_driven) steps.
- [Public intake IDOR & enumeration](public-intake-idor-enumeration.md) — public apply/lead endpoints must NOT trust client leadId (re-derive by email+source); uniform 409 ACCOUNT_CONFLICT; return leadId only when created===true.
- [i18n mandatory for ALL UI text](i18n-mandatory-all-ui.md) — NEVER hardcode any user-visible string; ALWAYS use t() with keys in all 10 lang files (en/tr/ar/fr/ru/fa/zh/hi/es/id); format: t("ns.key", { name: val }) with {name} interpolation.
- [Deleting a user — FK blockers](edcons-user-delete-fk-blockers.md) — deleting agent's user 500s on restrict/no-action FKs (notes.authorId, applicationStageDocuments.uploadedBy, messages/conversations/broadcasts/templates); delete in a tx + clearUserReferencesAndDelete (null nullable, reassign restrict to actor).
- [Agent onboarding gate & "Later" contract](edcons-onboarding-gate-later.md) — onboarding-reachable endpoints MUST be in ALLOWLIST_EXACT or gate 403s; isOnboardingContractMandatory (pending AND now>=startOfDay(expiresAt)) is single source for gate+status; dismissible before deadline day, blocking on/after.
- [Document preview](document-preview-newtab.md) — shared useDocumentPreview hook: left-click=in-app modal, middle-click=background tab via real <a> (window.open=foreground, fails). Lead docs need blob href; revoke only on unmount.
- [api-server unreachable = empty lists](api-server-unreachable-empty-lists.md) — list shows 0 despite DB data + no console error = vite /api proxy 502 (api-server down); frontend renders [] not error. Restart api-server.
- [Agent bulk Excel import vs onboarding](edcons-bulk-import-vs-onboarding.md) — bulk import inserts agent rows directly (no email/user/signing); match email exact lower() scoped to visible branches, link default branch on create.
- [Agent onboarding auto-credentials + intake](edcons-agent-onboarding-credentials.md) — agents provisioned active+verified w/ emailed password (no 6-digit code); guard existing-user-by-email reuse by AGENT_ROLES; sign route must reject intake_pending (finalizeSign doesn't).
- [Autoscale heavy render → HTML 403](autoscale-heavy-render-decouple.md) — sync Chromium/CPU in request path crashes autoscale → edge returns HTML 403, no app log; decouple to lazy gen (idempotent compare-and-set, fail hard on missing input).
- [GCS upload on sign hot path → OOM 403](gcs-upload-sign-hotpath.md) — GCS uploadBuffer (≤30 s) on the sign POST OOM-kills autoscale → opaque 403; store signature as base64 TEXT in DB at sign time; upload to GCS lazily inside ensureSignedContractPdf on first PDF download.
- [Prod schema migrations live in api-server boot DDL](prod-schema-bootstrap-ddl.md) — deploy runs NO migration; idempotent raw SQL DDL in api-server src/index.ts on boot is the only prod migration path; Drizzle edit alone won't alter existing prod tables.
- [CSRF must be seeded client-side](edcons-csrf-cookie-bootstrap.md) — autoscale serves SPA HTML at the edge, bypassing Express, so server never sets csrf_token on page load; client must seed the double-submit pair itself; sendBeacon can't send the header (use keepalive fetch).
- [Background delivery claim vs delivered](background-delivery-claim-vs-delivered.md) — dispatch workers need separate lease + delivered columns; reusing one "done" timestamp as claim loses delivery permanently on crash-after-claim.
- [Reproduce prod-only React #185](prod-only-react-185-repro.md) — compare live bundle hash vs fresh build (identical ⇒ not stale deploy); run prod api-server+Playwright(Nix chromium)+kill in ONE bash cmd; #185 triggered by persisted localStorage or prod data, not by code/ErrorBoundary.
- [Edge WAF blocks data: URIs in JSON](edge-waf-datauri-json.md) — JSON body with a >~4 KB `data:` URI → opaque HTML 403 before Express (no app log); send bare base64, server-validate PNG magic+size.
- [Contract signature broken-icon PDF](contract-signature-render-stale-pdf.md) — broken sig in portal ≠ renderer bug; render wraps bare base64. Real cause: agents.contract_url locked to FIRST contract via isNull guard (re-sign never updates) + idempotent PDF; resend makes a NEW session so query signed_contracts across ALL sessions.
- [Service-fee visibility cascade](service-fee-visibility-cascade.md) — hideServiceFees must resolve up the WHOLE ancestor chain on backend (effectiveHideServiceFees in /api/agents/me); client can't traverse. Gate every agency-side surface, incl agent_staff.
- [API token auth mechanism](api-token-auth-mechanism.md) — Bearer `fas_live_` coexists w/ sessions (Bearer-first, store SHA-256, skips CSRF); central default-deny scope guard (method+path→scope) not per-route requireScope; tokens can't manage tokens; prod table via boot DDL.
- [Main-agency seal + number/filename in final PDF](main-agency-seal-final-pdf.md) — seal+contract_number only in final PDF via ONE shared renderer (sign/backfill/regenerate); body {{contract_number}} and download filename (<number>_signed.pdf) must share one source; THREE download routes (admin/public/agent-portal) easy to miss the 3rd; regenerate NULLs pdfObjectKey/evidenceHash/deliveryClaimedAt, never resends email.
- [agent_staff permission enforcement](edcons-agent-staff-perms.md) — enforced via requireAgentStaffPermission (NOT requireStaffPermission); no-op for non-agent_staff roles; already wired backend+frontend across all 7 perms.
- [Page-internal useAuth role array overrides route auth](agent-page-internal-useauth-role-redirect.md) — authorized user bounced to /en = the destination PAGE calls useAuth(true,[roles]) excluding the role; route ProtectedRoute is source of truth, use useAuth(true).
- [Removing a wizard step drops bundled feature](edcons-step-removal-bundled-feature.md) — deleting a multi-step dialog step can silently kill plain controls bundled inside it (e.g. notes textarea); re-home independent controls + mirror the disabled guard in the submit handler.
- [RBAC agent-source scope (Sprint A)](rbac-agent-source-scope.md) — non-admin staff blocked from agentId≠null records; agent sees only own (not sub-agents); helper in src/lib/rbac/agentSourceScope.ts.
- [Lead "converted" status → convert endpoint](edcons-lead-status-convert-binding.md) — status=converted must call the idempotent convert mutation (creates/merges student), not a plain status PATCH.
- [Agent lead assignment scope](edcons-lead-assignment-scope.md) — agent/sub_agent assign own lead to own agent_staff/self/unassign; agent_staff self-claim only; validate target via getAgentVisibleIds (IDOR).
- [Student transfer to sub-agent](edcons-student-transfer-subagent.md) — move WHOLE chain in one TX (student+apps+service_fees+leads agentId AND full origin via inferOriginFromAgentId); recompute commissions (parent keeps share); parent getAgentVisibleIds includes own sub-agents.
- [Agent-portal sprint decisions](agent-portal-sprint-decisions.md) — parent sees own+own sub-agents (sub=own-only); commissions keep parent share (agentId=parent, subAgentId=sub, no double count); cross-agency isolation absolute; re-derive scope server-side (IDOR).
- [Express /me route before /:param](express-me-route-order.md) — static segments like /staff-cards/me/... must be registered BEFORE /:userId routes or Express will match the param first; check with grep for "router.get" ordering.
- [Drizzle raw sql ANY(array) broken](drizzle-any-array-serialization.md) — `sql\`col = ANY(${jsArray})\`` serializes number[] as scalar → PostgreSQL error; use `inArray(col, db.select(...).where(inArray(...)))` subquery instead.
- [Notification dispatcher in_app-sync / email-async split](notification-dispatcher-sync-async.md) — in_app DB insert must be awaited (tests check it); email+WA sends must be fire-and-forget IIFEs; processInbound must await dispatchNotification so in_app lands before test assertions.
- [Portal credentials management](portal-credentials-mgmt.md) — DB-first AES-256-GCM resolver (portalKey key); adapter login uses setCredsOverride/clearCredsOverride override map (not env mutation); list endpoint uses batchPortalCredentialKeys() for N+1-free hasCredentials.
- [Portal process cred key + dropdown registry fallback](portal-process-cred-key.md) — processSingle/process-queued must call lookupAdapterKey(universityKey) before resolvePortalCreds; /university-portals must merge adapterMetadata() fallback for fresh PROD deploys.
- [portal-adapters kind field](portal-adapters-kind-field.md) — GET /portal-adapters registry entries need `kind` ("declarative"|"code") derived from family; adapterMetadata() only returns family, derive kind in the route layer.
- [Topkapı Step 3 select2 education](topkapi-step3-select2-education.md) — placeholder ("Seçim Yapın") false-verifies; educationLevel select2 maps by APPLIED-degree value (eduLevelCandidates), set via in-page jQuery val+trigger, await dependents.
- [Topkapı Step 3 visible-row text inputs](topkapi-step3-visible-row-fill.md) — `[]` names match a HIDDEN template + visible row; fill+verify the SAME :visible Locator (locateVisibleInput logs count+visibleIndex); twopulse binding needs focus→value→input&change(bubbles)→blur; graduationDate is twopulse-datepicker (type=text): read data-date-format, set full date (default 01.01.YYYY), Escape to close.
- [Topkapı Step 4 program selection](topkapi-step4-program-pick.md) — program select is a hidden select2 → use setSelectByValue (jQuery), not page.selectOption; disabled/"(Kontenjan Dolu)" option hangs 8s → capture disabled flag, fast-fail throw + list OPEN programs; dump script login needs {credentials}.
- [Topkapi adapter quirks](topkapi-adapter-quirks.md) — ALL-CAPS Turkish options (ÖZBEKİSTAN); esbuild __name in $eval; jconfirm "Exclusive bölge" on Step2 next; mode enum is dry/real not dry/live; worker typecheck topkapi $eval DOM errors are pre-existing.
- [Topkapı language-agnostic locators](topkapi-language-agnostic-locators.md) — portal is physically switched to English before program discovery, so EVERY Topkapı text locator must be bilingual (`/(Sonraki Adım|Next Step)/i`) or it 8s-times-out; program-full via option.disabled first.
- [Topkapı final-submit success proof](topkapi-submit-success-proof.md) — CURRENT rule: the application-save.php RESPONSE BODY decides (status/success/redirect/applicationId), always log status+first 600 chars + "SAVE REJECTED"; HTTP 200 alone ≠ success; success-url is best-effort externalRef; never touch DRY branch.
- [Portal upload non-JPEG image → JPEG conversion](portal-image-jpeg-conversion.md) — portals (Topkapı photo) accept only JPG; convert via sharp using CONTENT-detected format (not ext/mime) in the shared download core; whitelist raster formats, never touch PDFs, non-throwing.
- [Native dep via workspace pkg crashes prod bundle](esbuild-native-dep-via-workspace.md) — api-server esbuild bundles @workspace/* libs; a native dep (sharp) reached transitively must be a DIRECT api-server dep or it's bundled→boot MODULE_NOT_FOUND (dev/tsx hides it).
- [Declarative portal engine](declarative-portal-engine.md) — JSON step engine: check/radio/selectLabel/phone variants; TS union ↔ zod stepSchema parity; dry-run skips only click{final:true}, so submit clicks MUST be marked final.
- [Portal document stub-vs-content fix](portal-doc-stub-fix.md) — POST /documents replace guard must check fileKey||fileUrl before retiring old content-bearing records.
- [programMatch fold Step 7 fix](programMatch-yuksek-lisans-fix.md) — 'yuksek lisans' two-word form wrongly tokenises as 'bachelor'; fold Step 7 merges to 'yukseklisans'.
- [programMatch synonym dict](programmatch-synonym-dict.md) — SYNONYM_GROUPS must be folded single-token (spaces/hyphens silently no-op); append (hub bridges); expansion is gated fallback; SYN-DB1 test asserts a term is ABSENT.
- [programMatch exact-match + EN thesis](programmatch-exact-match-thesis.md) — exact fold(name)==fold(query) short-circuits conf 1.0 BEFORE margin gate (near-identical Thesis/Non-Thesis siblings else null); tez filters must detect EN "non thesis"/"thesis" (hasTezsiz first).
- [OpenAPI UserProfile schema vs session payload drift](openapi-userprofile-drift.md) — buildSessionUser returns fields not in OpenAPI spec (emailVerified was missing); always cross-check openapi.yaml + generated api.schemas.ts + dist rebuild (tsc -b) when adding session fields.
- [i18n namespace copy-paste gap](i18n-namespace-copy-paste-gap.md) — keys added to one namespace (e.g. programs.allCities) are silently absent from others (catalogPage.allCities); new UI keys must be added to ALL relevant namespaces × ALL 10 lang files simultaneously.
- [Mandatory-doc gate dual-path](embed-widget-doc-gate.md) — required-doc enforcement lives in BOTH on-site Programs.tsx AND the embed widget (generated JS string in embed.ts, restart api-server to serve); fix both; never hard-block (backend parks in missing_docs).
- [Widget JS regex escaping](widget-template-literal-regex-escape.md) — embed widget client JS is a template literal; regex escapes need DOUBLE backslash in source or they de-escape (`\p{L}`→crash, `\D`→matches letter D); don't touch escapes inside `${...}` (real TS regexes).
- [Catalog-driven phone-code dropdowns](catalog-driven-phone-code-dropdowns.md) — phone dropdowns source dial codes from the catalog, but per-file PHONE_CODES arrays double as the number parser, so don't delete them on migration.
- [Phone picker dial-code search](phone-code-picker-search.md) — in-app pickers render server results verbatim (client filter is offline-only fallback); dial-code search lives in the /public/countries `search` predicate (name OR digits-normalized dialCode), NOT the React component; widget filters client-side.
- [Inbox reply endpoint shared RBAC](inbox-reply-endpoint-shared-rbac.md) — POST /inbox/conversations/:id/messages serves WhatsApp+Messenger+IG; agent_staff intentionally 403 (STAFF+ADMIN only); loosening the gate touches WhatsApp authz — split per-channel instead.
- [Inbox bot lead capture concurrency](inbox-bot-lead-capture-concurrency.md) — maybeAutoReply is fire-and-forget; bot lead/doc writes need multi-key sorted advisory locks + recheck over ALL identifiers (no unique index); re-engagement template resolved by convention from message_templates, not ai_agent config.
- [Inbox bot auto-reply design](edcons-inbox-bot-autoreply.md) — WhatsApp Claude auto-reply: fire after webhook 200 (non-blocking), conditional-UPDATE claim for idempotency, code-side escalation gate before LLM; mock external ids must be globally unique (messages channel+external_message_id index).
- [Meta channel shared signature/config](meta-channel-shared-config.md) — Messenger+IG mirror WhatsApp: shared verifyMetaSignature+META_API_VERSION in inbox/channels/meta-shared.ts; WA delegates to it; new integration cards opt into i18n via i18nKey+metaWebhook flags.
- [Meta outbound + 24h window](meta-outbound-24h-window.md) — Messenger/IG outbound via shared sendMetaText (me/messages); recipient=externalContacts.externalId NOT phone; 24h window blocks free-text (409); bot has no Meta template fallback.
- [Stage-doc upload surfaces parity](edcons-stage-doc-upload-surfaces.md) — 3 separate UI panels POST to stage-documents; backend stage-behavior flags (requiresValidUntil/tracksOfferExpiry) must be mirrored in ALL or one path silently 400s.
- [Multi-account per channel](multi-account-per-channel.md) — channel_accounts + conversations.channelAccountId; resolveInbound/Outbound gate on isActive, null→legacy fallback; reuse configMasking mergeConfig (preserve "•" secrets); integrations UI uses customFetch NOT Orval/OpenAPI.
- [Bulk-import CSV deterministic parse](bulk-import-llm-parse-scaling.md) — LLM row-parsing of CSV silently returns 0 records on large files (input slice + max_tokens cap → truncated JSON); parse rows deterministically (SheetJS), LLM only for fuzzy header mapping.
- [Portal dry-test CLI reuse contract](portal-dry-cli-reuse.md) — pnpm portal:dry reuses production runner/adapters (never duplicate); runSubmission gained optional headless (default true); dry forced via mode="dry"; buildProfileFromApplication shares helpers with buildStudentProfile.
- [Portal experimental auto-process guard](portal-experimental-autoprocess-guard.md) — TWO auto-process loops (drain-once.ts + worker.ts) both call claimNext(allowlist); experimental guard (isExperimentalAdapterKey by KEY not family) must be in BOTH; manual API path bypasses intentionally.
- [Program-fallback orchestrator](program-fallback-orchestrator.md) — program_full→handleProgramFull supersedes app to first OPEN fallback; idempotency MUST be inside tx under pg_advisory_xact_lock(srcAppId); runner can't import api-server (audit/notif direct via @workspace/db).
- [Structured program_full (Kontenjan dolu)](portal-program-full-structural.md) — quota-full is structural not a throw; resolveTarget program_full branch MUST precede dryRun so dry-runs still surface it; meta jsonb written only on programFull.
- [SIT program language safety](sit-program-language-safety.md) — SIT createApplication must NOT fall back to full catalog when language filter empties the pool (would submit wrong-language app); return programMissing instead.
- [SIT GraphQL auth + shape + university-error class](sit-graphql-and-university-errors.md) — cookie alone → HTTP 200 data:null; attach X-XSRF-TOKEN(+bearer) via page.request-primary then relative in-page fetch; connection() tolerates nodes/edges/array/null; dumps redact PII; not-permitted uni = programMissing:false not program_missing.
- [Topkapı Step 3 mapping & gpa norm](topkapi-step3-profile-mapping.md) — Step-N fields empty = builder mapping gap (TWO builders: portal-runner + worker); normalize gpa once in buildProfile (normalizeGpaRange, never Number()); format grad by REAL DOM type.
- [Portal automation Run Now + enqueue](portal-automation-runnow-enqueue.md) — all enqueue paths go through enqueueIfEligible; dedup is racy SELECT-then-INSERT (no unique index) so wrap check+insert in tx + pg_advisory_xact_lock(appId, hashtext(uniKey)); 3 callers (create/PATCH/run-now).
- [Multi-portal membership routing (Phase 3)](multi-portal-membership-routing.md) — catalog-keyed junction routes submissions through multi-portal accounts; resolveAdapterKey junction→routes_via→own; enabled=false ignored; member vs 1:1 program rows isolated (Topkapı must not regress); restart api-server for boot DDL.
- [Monorepo typecheck command](monorepo-typecheck-command.md) — never `tsc -b --noEmit` on a referenced-project package (TS6310); use the pkg's `typecheck` script (tsc -p tsconfig.json --noEmit); known pre-existing edcons errors in searchable-select.tsx + Applications.tsx.
- [Portal program mapping bulk Excel](portal-program-mapping-excel.md) — per-uni xlsx template + import (raw paths not orval); merge-upsert into programOverrides (never deletes), folded-label match must canonicalize to option `v`; NO_LIVE_OPTIONS from cache only.
- [Multi-portal routing](multi-portal-routing.md) — is_multi_portal company + member.routes_via; resolveAdapterKey overrides adapter ONLY when routedVia≠null (NULL=legacy); routing never touches auto_process; PATCH company key/disable must cascade routes_via in a tx.
- [Portal Universities mgmt reuse](portal-universities-mgmt-reuse.md) — Universities-tab CRUD reuses /api/portal-universities/* (portalMgmt.ts) + customFetch; do NOT build the spec's /portal-automation/universities/* OpenAPI split.
- [portal_program_cache boot-DDL only](portal-program-cache-deploy.md) — new boot-DDL tables are absent from dev/test DB until the api-server workflow restarts; restart before running tests touching them (42P01 otherwise).
- [Manual portal submission](portal-manual-submit.md) — admin board enqueue reuses findActivePortalUniversity (no hardcode); skips credential gate (unlike auto-trigger); legacy per-app endpoint coexists (STAFF+client key); in-memory rate limiter persists across tests → burst test runs LAST.
- [portal_adapter_specs enabled-version invariant](portal-adapter-specs-concurrency.md) — one enabled version per key enforced 3 ways: partial unique index (schema+migration+boot DDL), per-key advisory lock, atomic version-assign-in-tx; never call self-transacting setEnabledSpecVersion inside another tx.
- [Dual portal profile builders + Step-3 normalization](portal-dual-profile-builders.md) — TWO SubmitProfile builders (portal-runner + worker) drift; map every Step-3 field in BOTH; never Number() GPA (use normalizeGpaRange); graduation year needs widget-type-aware formatting.
- [Drizzle journal dead after 0017](drizzle-journal-not-maintained.md) — migrations 0018+ are hand-written idempotent SQL, NOT in meta/_journal; don't regenerate/journal new ones; self-FK needs AnyPgColumn callback.
- [Exclusive-region + dual status sites](exclusive-region-status.md) — nationality exclusion skips portal (preventive+reactive), exclusive_region terminal (claimNext only picks queued); a new submission status has TWO derivation sites: resolveTarget AND portalAutomation runWithTimeout inline mapping.
- [Portal↔CRM university auto-linker](portal-crm-university-linker.md) — auto-links crm_university_id by TR-aware name match; one-way containment fallback (portal⊆CRM, ≤1 extra, unique, len≥5) for dropped city prefixes; stale=CRM missing/0-programs, expose explicit linkStatus (don't infer from name).
- [App created_source classification](edcons-created-source-classification.md) — two automation creation paths (portalAutomation fan-out + portal-runner fallback supersession); backfill is best-effort proxy (portal_submission presence, enqueued_by can't distinguish fan-out from manual); null→student.
- [Drizzle/node:test tx & error testing](drizzle-tx-rollback-testing.md) — assert.rejects must walk err.cause (drizzle wraps as "Failed query:"); force rollback via scenario-scoped BEFORE INSERT trigger (drop in finally); DDL $$body$$ can't bind params, use sql.raw.
- [Name-based program mapping tiers](name-based-program-mapping-tiers.md) — matching is label→CRM-name (no CRM IDs); University>General needs TWO maps checked uni-first (flat merge lets General win on same-CRM/diff-label); general key `__general__` reserved.
- [Ordered fallback chain match gate](fallback-chain-match-gate.md) — auto fallback step2 only picks near-identical same-language variants (matchProgram margin gate); seeded apps MUST set `level` or levelGroup filter empties the chain.
- [SIT GraphQL Supabase Bearer](sit-graphql-supabase-bearer.md) — /api/graphql = Supabase pg_graphql over Zoho tables; use Collection/edges/filter/first/offset not made-up fields (real data:null cause); auth = mint Bearer (password grant) + apikey.
- [SIT create = webhook, not UI](sit-create-webhook.md) — SIT createApplication = graphql-derive ids + dedup precheck + OPEN n8n webhook POST (not the dropdown modal); dedup key excludes program/country, typo col `acdamic_year`, dedup must FAIL CLOSED; never log webhook body (PII).
- [SIT flexible university matching](sit-flexible-university-match.md) — matchAllowedUniversity two-tier token-set/subset; combo pick = token-BOUNDARY membership + reject ties; program field is `university_name` (NOT `university`; that's on zoho_applications) + active:{eq:true}; stored English/bare spelling ≠ CRM name → filter GraphQL by CORE tokens (and-ilike) not full name; ilike can't Turkish-fold (aydin⊄Aydın) → diacritic misses need the distinct-universities diagnostic; UI combo is a typeahead.
- [SIT/United aggregator member routing](sit-united-aggregator-member-routing.md) — resolvePortalRouting: member→aggregator WINS over standalone; submission universityKey=aggregator, universityName=member, meta.targetUniversityName drives runner school-select; adapter via name fallback (routedVia null) is fragile-but-works.
- [SIT auto multi-university fan-out](sit-auto-fanout.md) — SIT-routed app auto-fans to all isSitMember unis by reusing fanOutApplicationToUniversities w/ routeVia:{universityKey}; env-gated SIT_AUTO_FANOUT; auto-path dedup adds "submitted" (idempotent); legacy manual path unchanged; no recursion, no matcher added.
- [User Activity internal-only scope](edcons-activity-internal-scope.md) — scope to @workspace/roles STAFF_ROLES in 5 paths (presence/analytics/modules[needs innerJoin]/user:id 404/pdf 404); PDF esc() decodes entities, branding Settings-only, Sessions KPI real COUNT(*) not limit-100 length.
- [Composite workspace packages need dist rebuild](composite-package-stale-dist-typecheck.md) — "no exported member" for a just-added export in a referenced lib/* pkg (composite:true) = stale dist/, rebuild w/ tsc -b, not an import bug.
- [SIT country/nationality = zoho_countries ID](sit-country-dropdown-id.md) — SIT dropdown fields want the zoho_countries ROW ID not the name (raw name → INVALID_DATA:Nationality1); resolveCountryId(ilike+fold), fail-safe on ambiguous multi-match (null, never nodes[0]).
- [SIT membership vs catalog](sit-membership-vs-catalog.md) — catalog≠member; isSitMember=allowlist∪env; runner skips non-members pre-login (skippedNotMember/routeTo:direct); no-migration maps to exclusive_region+meta.reason; GPA optional never throws.
- [SIT auth single-session + token grant](sit-auth-single-session.md) — never submit login form (trips captcha); mint Supabase token via grant, cache process-level per email; ALWAYS GET origin for XSRF cookie; env refresh gated on token validity not presence.
- [Embed Step-1 early lead](embed-step1-early-lead.md) — fire lead on name+email basics BEFORE phone validation blocks Next (lead_form excluded); deduped 201 returns leadId:null = refreshed row, not failure.
- [Quick-contact dispatch scope](quick-contact-dispatch-scope.md) — outbound-on-behalf-of-entity endpoints: reload entity, agent-source scope 404, entity-stored phone only; 24h window on EVERY outbound path incl. Zernio.
- [Latin-name coverage + univ filter source](edcons-latin-name-and-univ-filter.md) — Latin reject at embed/students/leads/public-apply/inbox-staff; bot auto-capture (leadCapture/processInbound) EXEMPT (pipeline, no lost leads); submission univ filter = canonical portal_universities INNER JOIN, no raw fallback.
- [Self-fill link shows localhost](selffill-link-origin-rebuild.md) — getAppBaseUrl falls to http://localhost:5000 when domain env absent at boot; admin-copied/displayed signing links must be rebuilt with window.location.origin (SelfFillLinks toBrowserSignUrl), /sign route is at origin root.
- [United live-submit hardening](united-adapter-live-hardening.md) — multi-step ASP.NET/Salesforce wizard (not a webhook); create-POST instrumentation must default-deny redact body VALUES (keys+structural ids only, never PII); dry stops at Program→Personal (spec-locked, no guard).
- [Public doc intake: data-URL + lead-first](public-doc-intake-dataurl-leadfirst.md) — public base64 doc endpoints must strip data:URL prefix before decode AND never 400 before the lead is saved (drop bad docs w/ documentWarnings).
- [Unmatched inbox notification rule](inbox-unmatched-notification-rule.md) — event renamed to inbox.message_unmatched, email OFF by default; legacy inbox.unmatched row kept inert (deactivated), never dispatched again.
- [Topkapi fuzzy program-match fallback + goto retry](topkapi-fuzzy-fallback-and-goto-retry.md) — local noise-stripped fallback reuses programMatch.ts synonym dict via new additive (behavior-unchanged) exports, never edits matchProgram() itself.
- [Local storage public-objects mismatch](local-storage-public-objects-mismatch.md) — local driver has no "public/" subfolder; outbound senders must resolve keys via shared searchPublicObject, not hand-rolled URL parsing.
- [Topkapi GPA scale + parent-name guard](topkapi-gpa-parent-name.md) — topkapiGpaTo100 converts normalizeGpaRange output to 0-100; fatherName/motherName use "-" fallback (not "") in both profile builders.
- [Altınbaş adapter (Faz 1)](altinbas-adapter-faz1.md) — SF Experience Cloud + Screen Flow; level guard Master/PhD; self-capture after Step 1; Faz 2 = program select from dry-run logs.
